{
  "name": "devanswers-backend",
  "version": "1.0.0",
  "description": "The goal of this project is to implement a backend for a Q&A platform using MongoDB and Mongoose.",
  "type": "module",
  "scripts": {
    "queries": "node queries.js",
    "populate": "node scripts/populate_db.js",
    "run1": "npm run populate && npm run queries",
    "run": "nodemon --exec \"npm run populate && npm run queries\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "mongoose": "^8.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
